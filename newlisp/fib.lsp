#!/usr/bin/newlisp

(define (fib n)
  (cond
    ((= n 0) 0)
    ((= n 1) 1)
    (true (+ (fib (- n 1)) (fib (- n 2)))))
  (if true 100))

(sexp1 2
  (println 30))
;
; (println (fib (int (main-args 2))))

(setq t [text]
Some multiline
text
[/text])

(println t)

(exit)
