library(tidyverse)
library(dplyr)

# h2 <- read.csv("/home/denis/sandbox/datasets/h2.csv", header=TRUE, sep="\t")
# View(h2 %>% filter(attack > 10, race == "Knight" | race == "Wizard") %>% arrange(desc(defence)))
